{
  "$schema": "https://turbo.build/schema.json",
  "globalEnv": ["API_BASE_URL", "FRANCHISE_REGISTRY_CONTRACT"],
  "pipeline": {
    "build": {
      "dependsOn": ["^build"],
      "outputs": ["dist/**"]
    },
    "lint": {},
    "lint:fix": {},
    "dev": {
      "cache": false,
      "persistent": true
    }
  }
}
